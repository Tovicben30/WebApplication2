@{
    ViewBag.Title = "Home Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="jumbotron">
    <div class="row">
    <div class="text-center">
        @using (Html.BeginForm("Stories", "Home"))
        {
        <h2>
            See the stories already created or build your own.
        </h2>



            <div class="btn btn-success">
                @Html.ActionLink("Project","Project", "Home")
            </div>
            <div class="btn btn-success">
                @Html.ActionLink("Home Stories","Home")
            </div>

        }
    </div>
</div>
</div>